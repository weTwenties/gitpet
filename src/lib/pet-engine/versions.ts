/**
 * Algorithm versions. Bump explicitly whenever generation logic changes,
 * so the same username never silently rerolls its pet.
 */
export const IDENTITY_VERSION = 1;
export const FUSION_VERSION = 1;
export const ASSET_VERSION = 1;
